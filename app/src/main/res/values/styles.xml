<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="android:windowBackground">@drawable/dva</item>
        <!--<1item name="android:windowBackground">@drawable/pic_loading</item>-->
    </style>

</resources>
